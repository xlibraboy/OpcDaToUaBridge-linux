# Taste
- Prefers graceful failure isolation across independent connections/entities — when the app talks to several peers at once (e.g. multiple OpcBridge instances), one unreachable peer must not abort the healthy ones; each peer reports its own live status (Connected / Reconnecting / Failed plus the error text) and failed peers are auto-retried so one that comes up later joins, instead of an all-or-nothing connect. Confidence: 0.6
- Wants multi-instance scenarios treated as real, first-class use cases — the HMI should connect to several bridges at once with several OPC UA sources per bridge (not a single-bridge/single-source model), and the test rig should exercise that shape with multiple service/sim containers rather than a single-instance smoke test. Confidence: 0.5
- Delegates git commit and push of completed work to the agent; wants changes committed and pushed to origin (main) when work is finished (explicitly asked to "commit and push" after a task was done on multiple separate occasions, including again after this task). Confidence: 0.9
- Prefers Docker as the source of truth for toolchains/SDK versions and for running the app — when a project has a Dockerfile, read it to determine the SDK/base image and build/run in Docker rather than relying on host-installed tooling (e.g. "for .net sdk please see on docker"; again explicitly asked "please run with docker" instead of host `dotnet run`). Confidence: 0.9
- Wants runtime/service state checked via Docker — when asked whether a service is running, inspects containers (`docker ps -a`, including stopped ones) rather than host processes, because the user runs services as containers ("please check on the docker"). Also refers to specific services by their container name (e.g. "i think the name container is 'opcbridge-run2'") and expects the agent to enumerate existing/stopped containers and find them by name rather than inferring existence from image names or registries. Confidence: 0.8
- Wants answers scoped strictly to the component/service asked about; corrects the agent when it answers about a different one (e.g. clarified "OPC UA sim server only" after the agent reported on the DA simulator). Confidence: 0.5
- Wants UI/config actions to be idempotent: when a button (e.g. "Use" on a discovered OPC UA server) adds something to a configuration list, it should auto-add only if not already present, and otherwise just select/use the existing entry without creating duplicates. Confidence: 0.6
- Wants distinct config entries to never overwrite each other: entities that share a common key (e.g. two OPC UA servers on the same host) must get unique identities (Source ID derived from host+port), so adding the second one never silently clobbers the first. Confidence: 0.8
- Wants the form-fill "Use" flow to NOT auto-save: it should populate the form as a new, unsaved source with an editable Source ID, and require the user to click Save — so the user keeps full control over the Source ID before anything is created. Confidence: 0.9
- Wants selectors/indicators of "selected" config entities (e.g. the Selected source dropdown) to reflect only persisted state — while editing a new, unsaved entry the selector should be empty rather than showing the previously selected name; it should only populate after the entry is actually saved. Confidence: 0.6
- Wants the identifier that keys a config entity to its mappings (e.g. Source ID used in node IDs) to remain locked/uneditable once the entity is saved, since renaming it would break the mappings — the value should be set at creation time instead. Confidence: 0.7
- Wants timestamps displayed on HMI screens to include millisecond precision (formatted like `yyyy-MM-dd HH:mm:ss.fff`); explicitly asked to show ms on the faceplate timestamp. Confidence: 0.6
- Reports UI issues by attaching screenshots (often directing attention to a specific element, e.g. "focus on 'DECIMALS'") and expects the agent to inspect the visual state in the image to diagnose rather than just re-asserting the code is correct. Confidence: 0.7
- Runs GUI apps in this project (HMI and its designer) as Docker containers forwarded to the host X11 display using a consistent run recipe — `--network host`, `-e DISPLAY=:0`, XAUTHORITY, XDG_CACHE_HOME/XDG_RUNTIME_DIR, `/tmp/.X11-unix` mount, and `--user "$(id -u):$(id -g)"` — reusing the same setup for each GUI app rather than inventing a new one. Confidence: 0.6
- Wants supporting infrastructure/services (e.g., InfluxDB) deployed as Docker containers with initial credentials/org/bucket/token provisioned, so the app can connect to them — not installed on the host ("can you install to docker? so i can connect later on with url, org, bucket, token"). Confidence: 0.8
- Wants UI elements sized so their text is fully visible — treats clipped/truncated text (e.g. an input whose placeholder "off (full precision)" is cut off because the field is too narrow) as a bug to polish, expecting the width to be enlarged to fit the content. Confidence: 0.5
- Prefers discovery/scan-based configuration for external services over typing full URLs: asked for an InfluxDB "Scan" button (mirroring the existing OPC UA discover pattern) so only an IP/host needs to be entered and the app probes the server and auto-fills the URL; also said "yes please" when offered to make the HMI Designer auto-detect the bridge's container publish port instead of the user typing the Store URL by hand. Confidence: 0.6
- Prefers type-derived defaults over arbitrary fixed types for numeric ranges — when asked about min/max for trend Y-axis, explicitly said "Min Max type are based of the tags type (not fiz used double)", meaning use the natural range of the tag's DataType (e.g. 0–65535 for UInt16, 0–1 for Boolean) rather than a generic double range. Confidence: 0.8
- Prefers a "dashboard sets, designer consumes" workflow for tag metadata like Unit — the web dashboard is where the user types the engineering unit name per tag (stored in TagMapping), and the HMI designer reads it from the tag cache; the designer may offer a manual override but the dashboard is the primary entry point ("we type from web dashboard, so from designer can get the unit name from dashboard"). Confidence: 0.8
- Prefers tag display properties (unit, label) to be configured in the web dashboard, not the HMI designer — the dashboard faceplate (Tags → Maps → tag → Setup tab) is the natural place for tag-level metadata entry; the designer consumes it. Confidence: 0.7
- Wants two-state ("digital") status behavior to be a per-tag setting on the tag mapping itself (own "digital" flag plus custom on/off text), not a per-widget/faceplate property, because the same tag's status is rendered in multiple HMI places. Confidence: 0.6
- Expects handling for tags that carry boolean meaning in non-Boolean types — some tags store true/false as a Byte (0–255) using only the 0/1 bits, so those must be presentable as on/off, with 1/true = on and 0/false = off as the default. Confidence: 0.6
- Prefers per-developer/local config and secret-y files kept out of version control via `.gitignore`, while shared project-level config stays committed — accepted the agent's offer to add the tool's local-scope settings file (e.g. `.commandcode/settings.local.json`) to `.gitignore` so personal overrides can't be committed by accident. Confidence: 0.5
- When a design or tooling choice is genuinely open (or they don't know the best practice), asks the agent for its recommendation and expects a recommended default with trade-offs explained rather than a blind implementation — e.g. asking for the preferred selection mode ("select manually or automaticly, whats the prefer is") and, when offered a config option, saying "i dont know what the function is and what the recomended is" so the agent explains the purpose and the documented recommended setup before they decide; also asks "please give me the best suggestions" when a UI restructuring is open. Confidence: 0.7
- Expects the built application to actually be launched so changes can be inspected/exercised live, not just compiled and unit-tested — repeatedly says "please run the app" (also when the app is already serving) and expects to be told how to reach it, e.g. the URL/port; often sends this as a bare one-line request ("pleae run it" / "please run it") with no other instruction. Confidence: 0.85
- Tests the bridge end-to-end with real tags from the OPC UA simulator container (`opcuasim`) rather than only unit tests — said "I want test with some tags, can you do it, you can use opc sim container", i.e. expects the app pointed at the sim so behavior can be exercised with live tag values. Confidence: 0.6
- Works with git worktrees kept in a project-local `.worktrees/` folder (explicitly asked to "use .worktrees folder" when creating a branch), not in the agent's managed worktree location — expects the repo's existing convention (directory named from the branch with slashes replaced by dashes, e.g. `.worktrees/feature-add-group-setup`) to be followed, and `.worktrees/` to be gitignored. Confidence: 0.8
- Names feature branches with a `feature/` prefix describing the work (e.g. `feature/add-group-setup`, `feature/polish-web-dashboard` — slash-separated, hyphenated words). Confidence: 0.7
- Wants the whole set of related apps running together, not just the one component that was changed — after the bridge was launched they immediately asked to "run the OpcBridge HMI and OpcBridge Designer too", i.e. the full suite (bridge + HMI + designer) up at once for end-to-end inspection; asks for the client apps as a pair in one bare request ("please run OpcBridge Designer and HMI"). Confidence: 0.65
- Wants client apps exercised against a fake/mock upstream service rather than only the real backend — asks for a stand-in bridge to be brought up so the HMI/Designer can be pointed at it and driven end-to-end ("please connect with a fake opcbridge (container)"), and expects that fake to be a container like the real service. Confidence: 0.5
- Treats demo/seed data and other set-up state as test-only, not permanent: expects test fixtures to live as runtime state (or in a reproducible seed script) rather than being baked into the product's shipped defaults or committed config, so a fresh instance still reverts to stock defaults. Confidence: 0.5
- Expects simulated/demo tag values to actually change on their own over time — treats a status/boolean tag whose value stays constant as a defect ("for boolean why is not update (change atomaticly)") and wants live, self-changing values (e.g. status nodes that toggle on their own) so the HMI visibly updates. Confidence: 0.7
- Prefers simulation dynamics to be fixed at the source rather than worked around: extend the simulator to expose real changing nodes of the needed types (Boolean/Int as well as Double) that a client reads normally, instead of static fixed values or an external script pushing values through the API on a timer. Confidence: 0.5
refers field explanations kept compact — long static help text should live in info tooltips so each label/value row stays on one line, with only dynamic, per-selection hints left inline next to the field. Confidence: 0.5
- Treats demo/seed data and other set-up state as test-only, not permanent: expects test fixtures to live as runtime state (or in a reproducible seed script) rather than being baked into the product's shipped defaults or committed config, so a fresh instance still reverts to stock defaults. Confidence: 0.5
- Prefers incremental polish in place over replacing the app's visual language: after an authored full redesign (new palette/tokens/type stack, restyled surfaces beyond the one dialog in question) was delivered for a "polish this dialog" request, the user discarded all of it ("please revert all") — keep the existing design language and improve the targeted surface rather than authoring a new design system. Confidence: 0.5
- Expects code changes scoped to the exact surface requested — bundling unrelated fixes and enhancements (unstyled wizard headers, checkbox/number-input styling, a11y additions) into the same change set contributed to a full revert; keep diffs limited to what was asked. Confidence: 0.5
- Keeps canonical container launch recipes in the repo's `.commandcode/settings.json` and expects them to be used verbatim — when recreating a container (e.g. after it was removed), find and reuse the exact `docker run` recipe from the project settings rather than improvising flags. Confidence: 0.6
- Runs Docker tooling containers as the host user (`-u $(id -u):$(id -g)`, `-e HOME=/tmp`) so build output isn't left root-owned, and treats root-owned files in the working tree as a problem to clean up so the user's own container builds can overwrite them. Confidence: 0.6
- Wants release notes/changelogs organized per app target/deliverable — each app (e.g. OpcBridge HMI, OpcBridge Designer) keeps its own changelog file listing only the releases that changed it, instead of one monolithic changelog covering everything. Confidence: 0.6
- Before committing to a structural/convention choice, asks whether it is "standard" ("does it standard if we do like that") and expects the agent to confirm the approach is an idiomatic/industry-standard pattern (e.g. one CHANGELOG per released artifact as with release-please/changesets/nx) and explain how to manage it, rather than just implementing it silently. Confidence: 0.5
- Prefers splitting changelogs by app while keeping a single lockstep product version (one shared version, one tag/release, one installer) over giving each app independent versions/tags/cadences. Confidence: 0.5
- Wants stale local branches whose upstream remote has been deleted cleaned out of the repo, issued as a bare terse one-liner ("clean the dead branches") and expects the agent to work out the safe procedure itself: remove the fully-merged ones with the non-destructive delete that refuses unmerged work, and surface/confirm anything holding commits not in `main` instead of deleting it silently, then report the resulting branch/worktree state. Confidence: 0.55
- Prefers deployment-specific values to be externalized to configuration with graceful absence rather than hardcoded literals in code: when offered design options for the CREATOR/SECTION split, chose to add a config section (appsettings.json `Profile`) and have the UI box hide when those values are empty, so a different deployment/shipped build omits the feature cleanly without a code change — picking this over keeping hardcoded literals behind a bool flag or an explicit profile-type setting. Confidence: 0.5
- Wants data that belongs to only one context pulled out of a shared list into its own separate block that renders only in that context — asked to "make it separate" for CREATOR/SECTION "because it only show for personal profile", i.e. don't leave context-specific fields listed unconditionally on a shared page; the shared surface should stay clean and nothing should look missing when the block isn't shown. Confidence: 0.5
- Requests design polishing in terse, non-native-English phrasing and expects the agent to infer the intended visual change rather than asking for clarification — inspects the canvas/app to reconstruct the intent (naming elements by their on-screen labels, e.g. CREATOR/SECTION) before/while implementing. Confidence: 0.5
- Reports failures hit while using the app as bare symptom questions ("why i couldnt login with the admin/admin") and expects the agent to investigate the running system — server logs, live state, and a real-browser reproduction — to establish the true root cause instead of guessing at credentials or user error. Confidence: 0.5
- When offered a menu of optional follow-up actions, replies with a terse blanket approval ("please do both") meaning carry out the whole offered list rather than picking one item — expects every proposed step done without being re-scoped, and the reply's wording may not match the number of options, so interpret it as "do them all". Confidence: 0.5
- Prioritizes actually getting the app running over strict adherence to the Docker workflow: when the container engine is genuinely unavailable, prefers a native host run (installing the SDK user-locally, no sudo) instead of blocking or waiting for Docker to be installed ("can you run without docker"). Confidence: 0.55
- Maintains a project-root `context.md` as the agent's context/instruction file and expects the agent to consult it to find the current next action — points the agent there with a bare instruction ("please read the context.md for the next action") rather than restating the project state. Confidence: 0.6
- Wants replicated UI/canvas components to follow the source app's real design tokens instead of literal screenshot pixels: when a spacing looked arbitrary (the warning banner's buttons had a 90px gap), questions it ("i mean shoul they have gap like that") and expects the agent to trace it back in the app's source (it was a CSS `margin-left: auto` artifact) and use the intended token (a 12px gap) on the canvas — a pasted screenshot may itself be zoom-scaled, so don't treat measured pixels as the spec. Confidence: 0.55
- Follows a design-then-implement workflow: UI components are prototyped/redesigned on the Pencil canvas from a screenshot reference, then the user asks for that finished design to be ported into the real application source ("i want you replace the OpcBridge web dashboard banner with the new one"; again, right after a canvas rebuild, a bare "now please modify on the OpcBridge web dashboard") — the canvas mockup is treated as the spec and the shipped app code must be brought in line with it. Confidence: 0.6
- Expects the agent to connect to the pen.dev MCP server as the tooling channel for Pencil canvas work, and issues the connection as part of the instruction before the actual canvas edit (e.g. "please connect to pen.dev mcp server, then reposition the 'login' user"; again "please connect to pen.dev mcp, check the canvas") — connect first, then inspect the existing canvas contents (including a pasted screenshot acting as the reference) before making the requested change. Confidence: 0.6
- Wants accessibility treated as a first-class pass across every UI surface, not just the web dashboard — after a design review, chose to run the dedicated a11y pass on the desktop HMI, i.e. mouse-only interactions (colour/emphasis toggles, splitter collapse, chart pins) and icon-only/glyph buttons with no accessible name are defects to fix, expecting keyboard equivalents, focus-visible rings, accessible names/help text, live-region announcements for status text, and Escape-to-dismiss on popups. Confidence: 0.5
- Works through design/audit findings mode-by-mode: expects a review to yield a prioritised, actionable fix plan (grouped findings plus a recommended order of follow-up passes) and then executes that plan one mode at a time, rather than asking for everything to be fixed in a single pass. Invokes each pass as a bare named design mode with no further instruction, following the review's own findings order (e.g. `/design relayout` after an `a11y` pass had already landed, then `/design recolor` as the next pass), and expects that pass to stay scoped to its own concern — a relayout pass handles layout/composition findings and leaves colour and typography to their own passes, a colour pass handles palette/token findings. Confidence: 0.6
- Wants tag selection/picker lists to show tag metadata only (bridge / source / tag name) with no live value column, so streaming value updates can't disturb the user's selection ("for create/ add tag to trend group please show the tags without the updated value"). Confidence: 0.6
- Wants persistent, named entities the operator manages (e.g. trend groups) to live on their own dedicated page with a list plus an editable name, instead of existing only as transient popups/dialogs ("i want trend group have own page, so the trend group have an list and a name"). Confidence: 0.5
- Prefers saved UI entities to capture the full operator-adjusted display state — per-item colour, visibility, Y-axis range/auto, layout mode — so reopening restores exactly the view it was left in, rather than re-deriving it from defaults (chose the "Everything" option for what a trend group saves). Confidence: 0.5
- Wants the repo's architecture/context documentation (`context.md`) kept in sync with the code when a change makes it stale — accepted the offer ("yes please") to refresh the reference graph, project-map rows and API notes after a feature branch altered them, rather than leaving the doc describing the old state — including correcting concrete stale facts (test count, "verified as of" commit/date), not just prose; again at the end of a feature cycle the request was just "please delete the old branch and update", expecting the previously-flagged stale snapshot to be re-stamped. Confidence: 0.65
- Wants every tag to carry its own min/max range (a per-tag display range) so HMI trends open already scaled to that tag's range — "i want make all tags have own range (min-max) ... so if we open the trends from OpcBridge HMI it already have own range (except auto range)" — with "Auto range" kept as the exception for tags/widgets where a fixed range isn't wanted. Confidence: 0.6
- Wants a configured tag range to act purely as a display target (the trend Y-axis scale) and never affect recorded data — decided "the tag range act purely as the axis target", i.e. values stay raw at the logging/write path (no clamping or filtering on write) because a scale is a display concern, not a measurement one. Confidence: 0.6
- Preserves existing fallback behavior when layering an override on top of it: for tags with no explicitly-set value, keep the current derived fallback rather than replacing it — confirmed "for tags with no explicit range, we keep the current type-derived fallback", so an override is added without changing the behavior of everything that lacks one. Confidence: 0.55
- Wants config UI gated by the tag's data type: fields/rows that only apply to certain types should be hidden (or otherwise blocked) for the rest rather than shown inertently — e.g. the Digital on/off-text config should only appear for Boolean or Byte tags and be blocked for other types, and follows through by asking for the same treatment on other fields (again requested that Deadband, Decimals and Unit be hidden for Boolean, and Decimals hidden for Byte/integer types). Confidence: 0.7
- Treats a selector offering an option that is a no-op for the current context (e.g. "Analog – show raw value" on a Boolean tag, where Auto already means raw) as an ambiguity defect to remove, preferring type-aware choices that only offer the meaningful alternative instead of a tri-state that overlaps with Auto. Confidence: 0.6
- Avoids UI wording that collides with established PLC/DCS terminology — objected to naming the "show raw value" mode "Analog" because in PLC/DCS an analog signal is a continuous process value (e.g. 4–20 mA), and expects options named for what they actually do ("Raw value — show the number") rather than reusing an industrial-automation term with a different meaning. Confidence: 0.7
- Expects a bare re-run request in a later session ("please run again") to fully re-establish the previously working end-to-end environment — first verify current state, then recreate whatever has died (containers may be gone after a host restart) and bring the whole stack back up (fake bridge + both client apps) with the same wiring, without being re-told the setup. Confidence: 0.5
- Expects the running stack to always be built from the latest commit: a run/re-run request explicitly says "with the last commit" (also phrased "please run the app based of the last commit") — check whether HEAD has moved past what the running binary/images were built from (and whether the app container still exists at all), then rebuild the app and GUI images and restart everything from that commit instead of just restarting a stale build. Confidence: 0.7
- Treats a visually inconsistent form/pane ("now are mixed") as a defect to polish: expects closely related rows to follow one layout grid — uniform label and control widths and row heights, with selects sharing the same aligned edge — instead of each field sizing to its own content. Confidence: 0.5
- Wants UI labels and orderings to match the system's real semantics, and raises it as a consultative question before changing anything ("for the topbar status pills of 'DA', should we rename and reposition?"): a status label must honestly describe the full scope of the data it summarises (a pill rendering an aggregate across all source types shouldn't carry one protocol's name), and status/inventory ordering should follow the app's actual pipeline flow (sources → bridge → UA) consistently with the other pages. Confidence: 0.5
- Expects UI/visual changes to be verified against the app running in a real browser before being reported done — rebuilt, relaunched, and checked with screenshots across both themes, several viewport widths (wide/tablet/mobile), and the relevant live data states, plus console/page-error checks — not merely a code diff or a successful build. Confidence: 0.6
- Wants dashboard status encoded redundantly rather than by colour alone: a state colour plus a text stamp/badge (and a shape/direction cue), so the state still reads in greyscale or colour-blind, and animations must collapse under `prefers-reduced-motion` while keeping the state information available. Confidence: 0.55
- Wants interactive dashboard elements to behave as real controls: keyboard operable, with a visible focus ring on tab-focus, and a screen-reader text alternative for a purely visual diagram. Confidence: 0.5
- Prefers a live dashboard to keep a stable DOM rather than re-render every poll: only rebuild markup when the underlying data signature actually changes, so running animations and the user's cursor/selection aren't reset on each refresh tick. Confidence: 0.5
- Wants honest first-paint and empty states: show an explicit "reading/loading" placeholder until real data arrives, don't present a not-yet-known state as if it were real, and don't paint unknown as a degraded/error state. Confidence: 0.5
- Treats a feature branch and its worktree as disposable scratch: once the work is no longer wanted, issues a bare "please delete this branch" and expects it gone outright even though it is unmerged — branch force-deleted, the `.worktrees/` checkout removed, and the container that was serving that build stopped, accepting that its port goes down. Because that is destructive, expects the agent to first verify and state exactly what would be lost (unmerged commits, checked-out worktree, running app) before acting. Confidence: 0.5
- Expects cleanup to be scoped to the resources tied to the request: leave unrelated containers, branches and worktrees alone and report them as untouched rather than tidying everything noticed (a stray unregistered worktree directory was flagged but not deleted). Confidence: 0.5
- Issues merges as bare one-liners ("please merge to main") and expects the agent to determine and follow the repo's existing merge convention (here explicit `--no-ff` merge commits titled "Merge feature/X into main") rather than choosing its own strategy. Confidence: 0.6
- Merges feature branches into `main` locally as part of the workflow; after a merge, report what landed, note that `main` is ahead of `origin/main`, and leave it unpushed unless explicitly asked to push. Confidence: 0.6
- Also wants finished feature branches themselves published to origin (pushed with upstream tracking) once the work is merged, so the full branch history exists on the remote as a record — accepted the offer to put the merged feature branch on origin with a plain "yes please". Confidence: 0.5
- Uses a personal global git identity and expects it applied at the global (`git config --global`) level — commits should be authored as "Budi Kurniawan <xlibraboy@gmail.com>", not under the hostname-guessed default author that git derives when no identity is configured. Confidence: 0.6
- Wants listings/reports annotated with timestamps: after being shown a plain branch list, asked "please show with timestamps" and expects each entry to carry its date/time (branch switch time from the reflog plus each branch's last-commit datetime) rather than just names/hashes. Confidence: 0.5
- Issues git requests as bare one-liners ("please commit") and expects the agent to decide the commit scope and message itself — commit only the changes belonging to the completed task, leave unrelated untracked files untracked unless asked, and say explicitly whether the commit was pushed or is still local. The wording is terse and can come out loosely/garbled ("please commit the push"), so whether push is included must be inferred from the phrasing and context rather than a fixed rule. Confidence: 0.7
- Distinguishes committing from pushing: a "please commit" request means commit only — no push — and the agent should report the resulting commit hash and diff stat and state plainly that everything is still local. Confidence: 0.5
- Commits follow the repo's Conventional Commits style with a type and scope plus a detailed body listing each change area (e.g. "fix(dashboard): stop the live table eating selections and make dialogs truly modal"), including a Co-authored-by trailer. Confidence: 0.5
- Wants unrelated bodies of work kept in separate commits rather than one mixed commit: when a dirty tree contained several independent efforts, chose the option to commit the main work together and the unrelated change (in another project) on its own — and separately tracked a standalone new file (AGENTS.md) as its own commit — because not mixing unrelated work matters more than keeping the number of commits minimal. Same choice again when the dirty tree mixed the just-finished feature with their own in-progress work touched in the same files: two commits (feature first, their work second) instead of one combined commit, with the shared-file hunks split so the feature commit carries none of their in-progress edits. Confidence: 0.6
- Accepts an unrelated in-progress feature being committed on the user's behalf as its own commit when it is what keeps the tree clean — including staging its previously untracked new source file (e.g. a new options class) so that commit still builds and stands on its own. Confidence: 0.5
- Approves adding legitimate, pre-existing project files that were simply never tracked (e.g. AGENTS.md, a Dockerfile for a project) as part of a commit-and-push request, while genuinely accidental junk (a stray file with a shell-redirect name, __pycache__/, lock files, test .trx results) is left out of the commit. Confidence: 0.5
write them. Confidence: 0.6
